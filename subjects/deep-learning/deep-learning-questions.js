const imgStyle = 'style="max-width:520px;border-radius:6px;border:1px solid #ddd;background:#fff;padding:4px"';
const img = (name) => '<div style="text-align:center;margin:10px 0"><img src="images/' + name + '.png" ' + imgStyle + '></div>';

const questions = [];

function single(id, m, q, opts, ans, exp) {
  questions.push({ id, m, type: 'single', q, opts, ans: [ans], exp, yq: false });
}
function fill(id, m, q, ansText, exp) {
  questions.push({ id, m, type: 'fill', q, ansText, exp, yq: false });
}
function short(id, m, q, ansText, exp) {
  questions.push({ id, m, type: 'short', q, ansText, exp, yq: false });
}
function compre(id, m, q, ansText, exp) {
  questions.push({ id, m, type: 'compre', q, ansText, exp, yq: false });
}
function draw(id, m, q, ansText, exp) {
  questions.push({ id, m, type: 'draw', q, ansText, exp, yq: false });
}

const m1 = '神经网络基础';
single(1, m1, 'M-P 神经元模型中，决定神经元是否被激活的核心条件是？', ['A. 输入样本数量是否足够多', 'B. 加权输入和是否超过阈值', 'C. 网络层数是否超过三层', 'D. 训练集是否已经归一化'], 1, 'M-P 模型把输入加权求和后与阈值比较，再经激活函数输出。');
single(2, m1, '单层感知机无法解决异或问题的主要原因是？', ['A. 异或问题不是线性可分问题', 'B. 异或问题没有输入特征', 'C. 感知机不能处理二分类', 'D. 感知机只能用于回归任务'], 0, '单层感知机本质是线性分类器，只能处理线性可分问题。');
single(3, m1, 'Softmax 常用于多分类输出层，主要作用是？', ['A. 把多个得分转换为概率分布', 'B. 删除所有负样本', 'C. 让网络只保留最大权重', 'D. 让梯度恒等于 0'], 0, 'Softmax 将各类别得分指数化并归一化，输出和为 1 的概率分布。');
fill(4, m1, '人工神经元通常先对输入做____求和，再经过____函数得到输出。', '加权|激活', '典型计算过程是 z=sum(wi xi)+b，随后 a=f(z)。');
fill(5, m1, '多层感知机通常包含输入层、____层和输出层。', '隐藏/隐', '输入层接收特征，隐藏层提取中间表示，输出层给出预测。');
fill(6, m1, 'Sigmoid 函数输出范围为____，Tanh 函数输出范围为____。', '0到1/0-1|负1到1/-1到1', 'Sigmoid 适合概率解释，Tanh 是零中心激活。');
fill(7, m1, 'ReLU 的表达式可写为 f(x)=____。', 'max(0,x)|max(0, x)', 'ReLU 在正区间梯度为 1，在负区间输出 0。');
fill(8, m1, '训练神经网络时，前向传播计算输出和损失，____传播根据损失计算梯度。', '反向', '反向传播利用链式法则把误差信号逐层传回。');
short(9, m1, '简述 M-P 神经元模型的基本思想。' + img('mp-neuron'), 'M-P 神经元把多个输入信号按权重加权求和，再与阈值比较，最后通过激活函数输出 0/1 或连续值。它用数学形式模拟生物神经元的兴奋与抑制，是人工神经网络中神经元模型的基础。', '答题要点：加权求和、阈值、激活函数、输出。');
short(10, m1, '为什么感知机可以看作线性分类器？', '感知机的决策函数本质上是 sign(wTx+b)，它在特征空间中对应一个超平面。样本位于超平面两侧时被划分为不同类别，因此它只能直接表示线性决策边界。', '关键是把模型形式和超平面联系起来。');
short(11, m1, '解释为什么隐藏层能增强神经网络的表达能力。', '隐藏层可以把原始输入逐层变换为更抽象的中间特征。配合非线性激活函数，多层网络可以组合出复杂的非线性映射，从而表示单层线性模型无法表示的关系。', '仅增加线性层不够，必须强调非线性激活。');
short(12, m1, '说明激活函数在神经网络中的作用。' + img('mlp-activation'), '激活函数为网络引入非线性，使多层网络不再等价于一个线性变换。它还影响梯度传播和训练稳定性，不同激活函数适用于不同场景，如 Sigmoid、Tanh、ReLU、Softmax 等。', '答题可从非线性、梯度、输出解释三个角度组织。');
short(13, m1, '比较 Sigmoid、Tanh 和 ReLU 的特点。', 'Sigmoid 输出在 0 到 1，适合概率解释，但两端容易饱和导致梯度小。Tanh 输出在 -1 到 1，零中心，通常比 Sigmoid 更利于优化。ReLU 计算简单，正区间梯度稳定，能缓解梯度消失，但可能出现神经元死亡。', '这是常见简答题，应写出范围、优点和缺点。');
short(14, m1, 'Softmax 的计算过程可以怎样概括？', 'Softmax 先对每个类别得分取指数，再把所有指数值求和，最后用每个类别的指数值除以总和。这样得到的输出都为正且总和为 1，可作为多分类概率。', '注意 Softmax 输出的是相对概率，不是独立概率。');
short(15, m1, '什么是交叉熵损失？为什么常用于分类任务？', '交叉熵用于度量真实标签分布和模型预测概率分布之间的差异。分类任务中真实标签通常可表示为 one-hot 分布，模型输出概率越接近真实类别，交叉熵越小；预测越偏离真实标签，损失越大。', '可结合 Softmax 输出概率说明。');
short(16, m1, '简述前向传播和反向传播的区别。', '前向传播从输入层开始逐层计算每一层的输出，最终得到预测值和损失。反向传播从损失函数出发，利用链式法则逐层计算参数梯度，为权重更新提供方向。两者配合完成一次训练迭代。', '前向算结果，反向算梯度。');
short(17, m1, '什么是梯度消失？它会造成什么影响？', '梯度消失指反向传播到较浅层时梯度变得非常小，导致前面层参数更新缓慢甚至几乎不更新。它会使深层网络训练困难，模型难以学习底层特征，常与饱和激活函数和长链式乘法有关。', '影响是训练慢、浅层难学习。');
short(18, m1, '什么是梯度爆炸？常见缓解方法有哪些？', '梯度爆炸指反向传播中梯度值不断放大，导致参数更新过大、损失震荡甚至数值溢出。常见缓解方法包括梯度裁剪、合适的权重初始化、归一化方法、降低学习率和使用更稳定的网络结构。', '与梯度消失相对，强调数值不稳定。');
short(19, m1, '为什么神经网络训练需要损失函数？', '损失函数把模型预测与真实目标之间的差异量化为一个可优化的标量。训练过程通过最小化损失来调整参数，损失函数也决定了模型优化方向和任务目标。没有损失函数，模型无法明确怎样算好或坏。', '损失函数是优化目标。');
short(20, m1, '说明学习率过大和过小分别可能带来的问题。', '学习率过大时，每次参数更新步长太大，可能越过最优点，造成损失震荡或发散。学习率过小时，参数更新很慢，训练时间变长，也可能停留在较差区域。合适学习率需要在稳定和速度之间平衡。', '可结合梯度下降画面理解。');
short(21, m1, '为什么权重初始化会影响深度网络训练？', '权重初始化决定了训练初期各层输出和梯度的尺度。如果初始权重过大，可能导致激活或梯度爆炸；过小则可能导致信号和梯度衰减。合理初始化有助于保持前向信号和反向梯度稳定。', '重点是信号尺度和梯度尺度。');
short(22, m1, '简述神经网络中偏置项 b 的作用。', '偏置项相当于调整神经元激活阈值或决策边界的位置，使模型不必强制经过原点。加入偏置后，神经元可以表达更灵活的函数关系，提高模型拟合能力。', '偏置不是噪声，而是可学习参数。');
compre(23, m1, '综合说明“多层线性变换仍等价于线性变换”，以及这对激活函数的启示。', '若每一层只做线性变换，例如 y=W2(W1x+b1)+b2，则可以合并为 y=(W2W1)x+(W2b1+b2)，仍是一个线性函数。无论堆叠多少层，只要没有非线性激活，表达能力不会本质增强。因此多层神经网络必须引入非线性激活函数，才能表示复杂决策边界。', '这是解释激活函数必要性的核心论证。');
compre(24, m1, '给出一个“神经网络训练一次迭代”的完整流程。', '一次迭代通常包括：取一个 batch 输入模型；执行前向传播得到预测；计算损失函数；执行反向传播得到各参数梯度；使用优化器按学习率更新参数；记录损失或指标。若使用验证集，还会定期评估泛化效果。', '按顺序写清楚即可。');
compre(25, m1, '如果训练损失长期不下降，可以从哪些角度排查？', '可以排查学习率是否过大或过小、数据和标签是否错误、输入是否需要归一化、模型容量是否不足、激活函数是否导致梯度消失、损失函数是否选错、权重初始化是否不合理。排查时应先看数据和训练曲线，再逐项调整。', '这是应用型综合题，强调系统排查。');
compre(26, m1, '比较均方误差和交叉熵在分类任务中的适用性。', '均方误差直接度量预测值与标签数值差异，更常用于回归；分类任务通常希望模型输出概率分布，交叉熵与极大似然思想一致，对错误且高置信度的预测惩罚更强，因此常配合 Softmax 或 Sigmoid 用于分类。', '不要绝对说 MSE 不能分类，而是说交叉熵更合适。');
compre(27, m1, '解释“模型容量过小”和“模型容量过大”在神经网络中的表现。', '模型容量过小时，网络层数或神经元数量不足，难以拟合训练数据，训练集和测试集表现都差，表现为欠拟合。模型容量过大时，网络参数过多，可能记住训练样本噪声，训练集很好但测试集较差，表现为过拟合。', '联系欠拟合和过拟合回答。');
compre(28, m1, '如何从考试角度组织“神经网络基础”这一章的答题框架？', '可以按“单个神经元、网络结构、激活函数、损失函数、训练算法、训练问题”六条线背诵。单个神经元讲加权求和和阈值；网络结构讲 MLP；激活函数讲非线性；损失函数讲优化目标；训练算法讲前向和反向传播；训练问题讲梯度消失、爆炸和学习率。', '适合期末速成的总括题。');
draw(29, m1, '画出 M-P 神经元模型并标注输入、权重、求和、阈值、激活函数和输出。', '图中应包含多个输入 x1,x2,...,xn；每个输入连接权重 w1,w2,...,wn；中间进行加权求和 sum(wi xi)；再与阈值 theta 或偏置 b 共同作用；最后经过激活函数 f 输出 y。', '作图题重点是结构完整，不要求与课件完全一致。');
draw(30, m1, '画出一个包含输入层、两个隐藏层和输出层的多层感知机，并说明信息流向。', '图中从左到右依次为输入层、隐藏层1、隐藏层2和输出层，相邻层之间全连接或按题意连接。信息在前向传播中从输入层流向输出层，训练时误差信号在反向传播中从输出层逐层传回。', '可用箭头标出前向传播和反向传播方向。');

const m2 = 'CNN与深度学习';
single(31, m2, '深度学习相对传统机器学习的重要优势之一是？', ['A. 完全不需要数据', 'B. 可以自动学习多层特征表示', 'C. 只能处理表格数据', 'D. 不需要损失函数'], 1, '深度学习的核心优势之一是表示学习，减少手工特征工程依赖。');
single(32, m2, '卷积神经网络最典型的应用场景是？', ['A. 图像识别和视觉感知', 'B. 只用于排序数字', 'C. 只能处理一维标量', 'D. 只能用于数据库查询'], 0, 'CNN 通过卷积核提取局部空间特征，适合图像任务。');
single(33, m2, '池化层的主要作用通常是？', ['A. 增大图像分辨率', 'B. 降低特征图尺寸并增强局部不变性', 'C. 删除所有卷积核', 'D. 将所有像素变为文字'], 1, '池化可降低计算量，保留重要响应，并增强平移不变性。');
fill(34, m2, '深度学习的核心是通过多层网络学习数据的____表示。', '特征', '深度学习常被理解为分层特征学习或表示学习。');
fill(35, m2, 'CNN 通常由卷积层、____层和全连接层等结构组成。', '池化', '典型 CNN 用卷积提取特征，用池化降维，用全连接或分类头输出。');
fill(36, m2, '卷积层中，用于在输入上滑动并提取局部模式的小矩阵称为____。', '卷积核/滤波器/filter/kernel', '卷积核也常称滤波器。');
fill(37, m2, 'CNN 的两个重要思想是局部连接和____共享。', '权值/参数/权重', '权值共享显著减少参数数量。');
fill(38, m2, '常见池化方式包括最大池化和____池化。', '平均', '最大池化保留最强响应，平均池化取局部平均。');
short(39, m2, '为什么深度学习能减少手工特征工程？', '深度学习通过多层非线性网络从数据中逐层学习特征表示，低层可学习边缘、纹理等简单特征，高层可组合出部件和语义特征。因此它不完全依赖人工预先设计特征，尤其适合图像、语音和文本等复杂数据。', '答题要点：多层、自动、分层特征。');
short(40, m2, '简述 CNN 的基本结构和数据流。' + img('cnn-architecture'), 'CNN 通常接收图像矩阵作为输入，经过若干卷积层提取局部特征，经过激活函数引入非线性，使用池化层降低空间尺寸，最后通过全连接层或分类头输出类别概率。数据流从像素到局部特征，再到高级语义表示。', '结构题要按层次说明。');
short(41, m2, '卷积操作在图像处理中如何理解？' + img('convolution-process'), '卷积操作可理解为卷积核在图像上滑动，对局部区域进行对应元素相乘并求和，得到输出特征图的一个位置。不同卷积核可以检测不同模式，如边缘、纹理、模糊、锐化等。', '重点是滑动窗口、乘加、特征图。');
short(42, m2, '为什么 CNN 使用局部连接？', '图像具有强局部相关性，相邻像素往往共同构成边缘、纹理等局部结构。局部连接让神经元只关注局部感受野，可以减少参数数量，利用图像空间结构，并逐层组合局部特征形成更复杂的表示。', '局部连接不是信息缺失，而是利用图像结构。');
short(43, m2, '解释 CNN 中权值共享的意义。', '权值共享指同一个卷积核在不同空间位置复用同一组参数。这减少了模型参数和计算成本，也让同一种局部模式无论出现在图像哪个位置都能被检测到，从而增强平移等变性。', '常考：减少参数、检测同类局部模式。');
short(44, m2, '最大池化和平均池化有什么区别？', '最大池化取局部窗口中的最大值，突出最强响应，常用于保留显著特征；平均池化取局部窗口平均值，更关注整体平滑信息。二者都能降低特征图尺寸和计算量，但保留信息的方式不同。', '不要只写公式，要写语义差异。');
short(45, m2, '什么是感受野？它在 CNN 中为什么重要？', '感受野指输出特征中某个位置所能看到的输入区域范围。随着卷积层堆叠，深层神经元的感受野变大，可以从局部边缘逐渐组合出更大范围的目标结构。感受野决定了模型能利用多大上下文信息。', '可联系局部到全局。');
short(46, m2, '说明卷积核大小、步长和填充分别影响什么。', '卷积核大小决定每次观察的局部区域范围；步长决定卷积核滑动间隔，步长越大输出尺寸通常越小；填充是在边界补值，可控制输出尺寸并保留边缘信息。三者共同影响特征图大小和信息保留。', '这是 CNN 计算类基础题。');
short(47, m2, '为什么 CNN 最后常接全连接层或分类头？', '卷积和池化层主要负责提取空间特征，最后需要把这些特征映射到具体任务输出。全连接层或分类头可以综合前面提取到的高层特征，输出类别得分、概率或其他预测结果。', '现代 CNN 也可用全局平均池化加分类层。');
short(48, m2, '深度学习取得成功的常见原因有哪些？', '常见原因包括大规模数据集的出现、GPU 等计算能力提升、网络结构和优化方法改进、非线性表示学习能力增强，以及开源框架降低了训练和部署门槛。这些因素共同推动深度学习在视觉、语音、自然语言等领域取得突破。', '从数据、算力、算法三方面回答最稳。');
short(49, m2, '传统图像识别流程和 CNN 流程有什么不同？', '传统流程通常需要人工设计预处理、特征提取和特征选择，再把特征交给分类器。CNN 则把特征学习和分类整合到端到端训练中，通过卷积层自动学习从低级到高级的视觉特征，减少人工特征工程。', '对比题要突出手工特征 vs 自动表示。');
short(50, m2, '什么是特征图？', '特征图是卷积核作用于输入后得到的二维响应矩阵。它表示某类局部特征在不同空间位置上的激活强度。多个卷积核会产生多个特征图，对应不同的边缘、纹理或更高级模式。', '特征图不是原图，而是特征响应。');
short(51, m2, '为什么 CNN 适合处理图像平移问题？', '卷积核在整张图像上共享并滑动，同一模式出现在不同位置时都会被同一卷积核检测到。池化还可以降低小范围位移带来的影响，因此 CNN 对图像中目标位置变化具有一定鲁棒性。', '可称为平移等变和局部平移不变性。');
short(52, m2, '简述 LeNet、AlexNet 等经典 CNN 的共同思路。', '这些经典 CNN 都通过卷积层提取局部视觉特征，通过激活函数引入非线性，通过池化降低空间尺寸，再用分类层输出类别。随着网络发展，层数更深、通道更多、训练技巧更成熟，图像识别能力不断提升。', '不必背具体参数，重点是结构思想。');
compre(53, m2, '综合说明 CNN 如何从像素逐层形成高级语义表示。', '输入层接收原始像素矩阵，浅层卷积核通常学习边缘、角点、纹理等低级局部特征；中间层组合这些低级特征形成局部部件；深层进一步组合为目标形状或类别相关语义。池化和步长操作逐渐压缩空间尺寸，使网络从局部细节过渡到全局判断。', '这是 CNN 分层特征学习的经典答法。');
compre(54, m2, '若一个 CNN 在训练集表现好但测试集差，可以怎样改进？', '这通常说明过拟合。可尝试增加数据或数据增强，减小模型容量，加入 Dropout 或权重衰减，使用早停，改进数据划分，检查训练集和测试集分布是否一致。也可以观察训练和验证曲线，判断过拟合从何时开始。', '把机器学习泛化思想迁移到 CNN。');
compre(55, m2, '设计一个简单图像分类 CNN，并说明每部分作用。', '可以设计为：输入图像；卷积层提取局部边缘和纹理；ReLU 引入非线性；池化层降低尺寸；再堆叠一组卷积和池化提取更高级特征；展平或全局平均池化；最后用全连接层加 Softmax 输出类别概率。', '结构合理、作用说明清楚即可。');
compre(56, m2, '为什么卷积可以用于边缘检测、模糊和锐化？', '卷积核本质上定义了一种局部加权组合规则。不同核权重强调不同像素关系：边缘检测核强调灰度突变，平滑核对局部像素取平均，锐化核增强中心像素与周围差异。因此改变卷积核即可实现不同图像处理效果。', '联系课件中的图像处理例子。');
compre(57, m2, 'CNN 中参数数量如何受到卷积核和通道数影响？', '一个卷积层的参数数量通常与卷积核高、宽、输入通道数和输出通道数有关，再加上偏置项。相比全连接层，卷积层通过局部连接和权值共享显著减少参数，但通道数增加仍会增加模型容量和计算量。', '无需复杂公式也要说明影响因素。');
compre(58, m2, '卷积层、池化层、全连接层在 CNN 中分别承担什么角色？', '卷积层负责提取局部空间特征，激活函数赋予非线性；池化层负责压缩特征图、降低计算量并提高一定鲁棒性；全连接层或分类头负责汇总高层特征并映射为任务输出。三者共同完成从图像到类别的端到端学习。', '按角色分工回答。');
draw(59, m2, '画出一个 CNN 图像分类流程图。', '图中应包含输入图像、卷积层、激活函数、池化层、若干重复特征提取模块、展平或全局平均池化、全连接层和 Softmax 输出。箭头从输入到输出，标注“特征提取”和“分类决策”两个阶段。', '流程图比画具体数值更重要。');
draw(60, m2, '画出 3×3 卷积核在图像上滑动并生成特征图的过程。', '图中左侧画输入矩阵，中间画 3×3 卷积核覆盖局部区域，标注对应元素相乘后求和，右侧画输出特征图的一个位置。再用箭头表示卷积核滑动到下一个位置继续计算。', '要体现局部窗口、乘加和输出位置。');

const m3 = 'RNN与循环结构';
single(61, m3, 'RNN 适合处理序列数据的关键原因是？', ['A. 它完全不使用参数', 'B. 它通过隐藏状态保留历史信息', 'C. 它只能处理静态图片', 'D. 它不需要训练'], 1, 'RNN 的隐藏状态把过去输入的信息传递到后续时间步。');
single(62, m3, '普通 RNN 难以学习长距离依赖，常见原因是？', ['A. 梯度消失或梯度爆炸', 'B. 输入一定是二维图像', 'C. 没有任何非线性函数', 'D. 标签数量必须为 0'], 0, '序列展开后链式传播很长，容易出现梯度问题。');
single(63, m3, 'LSTM 中用于决定旧细胞状态保留多少信息的是？', ['A. 遗忘门', 'B. 输出门', 'C. 卷积核', 'D. 池化层'], 0, '遗忘门控制上一时刻细胞状态中哪些信息被保留或丢弃。');
fill(64, m3, 'RNN 的中文名称是____神经网络。', '循环/递归', '本课件使用循环神经网络。');
fill(65, m3, 'RNN 在每个时间步通常接收当前输入和上一时刻的____状态。', '隐藏/隐', '隐藏状态携带历史信息。');
fill(66, m3, 'LSTM 的三个典型门结构包括遗忘门、输入门和____门。', '输出', '三门结构用于控制信息读写和输出。');
fill(67, m3, 'GRU 将 LSTM 中的遗忘门和输入门合并为____门。', '更新', 'GRU 用更新门和重置门简化 LSTM。');
short(68, m3, '什么是序列数据？举例说明 RNN 的应用。', '序列数据是元素之间存在时间或顺序关系的数据，例如句子中的词序列、语音帧序列、时间序列股票价格等。RNN 可以用于语言模型、机器翻译、情感分析、语音识别和序列预测。', '关键是顺序依赖。');
short(69, m3, '简述 RNN 的基本计算思想。' + img('rnn-unrolled'), 'RNN 在每个时间步接收当前输入 xt 和上一时刻隐藏状态 ht-1，通过同一组参数计算当前隐藏状态 ht，并可进一步产生输出 yt。由于隐藏状态不断传递，模型可以把历史信息带入当前预测。', '可写公式 ht=f(Wx xt+Wh ht-1+b)。');
short(70, m3, '为什么 RNN 参数共享？', 'RNN 在不同时间步复用同一组参数，这使模型可以处理任意长度序列，减少参数数量，并让同一种序列转换规律在不同位置都适用。如果每个时间步都用独立参数，模型会难以泛化到不同长度。', '时间维度共享参数是 RNN 的重要特征。');
short(71, m3, '什么是 RNN 展开？为什么要展开？', 'RNN 展开是把循环结构沿时间维度展开成多个相连的网络单元，每个单元对应一个时间步。展开后可以直观看到信息和梯度如何在序列中传播，也便于理解通过时间反向传播 BPTT。', '展开不是改变模型，而是便于计算和理解。');
short(72, m3, 'RNN 的短期记忆问题是什么？', '普通 RNN 在长序列中容易更关注近期输入，而较早输入的信息会在多次状态更新中逐渐衰减。再加上梯度消失问题，模型难以学习远距离依赖，这被称为短期记忆问题。', '可以举“句子前后相隔很远的依赖”。');
short(73, m3, 'LSTM 如何缓解普通 RNN 的长距离依赖问题？', 'LSTM 引入细胞状态作为相对稳定的信息通道，并通过遗忘门、输入门和输出门控制信息保留、写入和输出。门控结构让重要信息可以在较长时间内传递，从而缓解梯度消失和短期记忆问题。', '核心是细胞状态和门控。');
short(74, m3, '解释 LSTM 遗忘门的作用。' + img('lstm-gates'), '遗忘门根据上一时刻隐藏状态和当前输入生成 0 到 1 之间的权重，决定上一时刻细胞状态中每个部分保留多少。接近 1 表示保留，接近 0 表示遗忘，因此它负责清理无用历史信息。', '遗忘门不是全部删除，而是按维度控制保留比例。');
short(75, m3, '解释 LSTM 输入门的作用。', '输入门决定当前时刻的新信息有多少写入细胞状态。它通常与候选细胞状态配合：候选状态提供可写入的新内容，输入门控制这些内容的写入强度，从而更新长期记忆。', '输入门控制写入。');
short(76, m3, '解释 LSTM 输出门的作用。', '输出门决定细胞状态中哪些信息用于生成当前隐藏状态和输出。它先根据当前输入与上一隐藏状态得到门控权重，再对处理后的细胞状态进行筛选，使模型输出与当前任务相关的信息。', '输出门控制读出。');
short(77, m3, 'GRU 与 LSTM 的主要区别是什么？', 'GRU 结构比 LSTM 更简化，它通常将遗忘门和输入门合并为更新门，并使用重置门控制历史信息参与程度。GRU 没有独立的细胞状态，参数更少、训练更快，但表达方式与 LSTM 略有不同。', '答题要写更新门、重置门、参数少。');
short(78, m3, '什么是 BPTT？', 'BPTT 是通过时间反向传播，即把 RNN 沿时间展开后，从序列末端或损失位置开始反向计算梯度，并沿时间步逐步传回共享参数。它是训练 RNN 的基本方法，但长序列中容易出现梯度消失或爆炸。', 'BPTT = backpropagation through time。');
short(79, m3, 'RNN 可以有哪些输入输出形式？', '常见形式包括一对一、多对一、一对多和多对多。多对一可用于情感分类，一对多可用于图像描述生成，多对多可用于序列标注或机器翻译。不同任务决定输出发生在最后一步还是每个时间步。', '用任务例子加分。');
short(80, m3, '为什么自然语言处理需要考虑上下文？', '词语含义常由上下文决定，同一个词在不同句子中可能有不同语义。句子整体结构、前后词关系和长距离依赖都会影响理解。RNN 通过隐藏状态建模上下文信息，因此早期常用于自然语言处理任务。', '联系 RNN 的历史信息。');
short(81, m3, '普通 RNN、LSTM、GRU 三者如何选择？', '普通 RNN 结构简单，适合短序列或教学理解；LSTM 门控完整，适合长距离依赖较强的任务；GRU 参数更少、计算更轻，常在效果接近 LSTM 时作为更高效选择。实际选择需看数据规模、序列长度和验证效果。', '没有绝对最优。');
short(82, m3, 'RNN 在训练时为什么可能比前馈网络更难？', 'RNN 需要沿时间展开，计算图更长，隐藏状态反复递归，梯度传播路径长，因此更容易出现梯度消失、梯度爆炸和训练不稳定。序列长度变化也会增加训练和批处理复杂度。', '强调时间维度带来的长依赖。');
compre(83, m3, '综合比较 CNN 和 RNN 的适用数据与核心结构差异。', 'CNN 主要利用局部连接和权值共享处理具有空间结构的数据，如图像；它通过卷积核提取局部空间模式。RNN 主要通过隐藏状态处理具有时间或顺序关系的数据，如文本和语音；它通过循环连接保留历史信息。二者都共享参数，但共享维度不同：CNN 在空间位置共享，RNN 在时间步共享。', '这类横向比较常考。');
compre(84, m3, '给定一句话做情感分析，说明 RNN 如何完成预测。', '先把句子中的词转换为向量表示，按词序依次输入 RNN。每个时间步结合当前词向量和上一隐藏状态更新上下文表示。处理完整个句子后，可以取最后隐藏状态或聚合所有隐藏状态，再送入分类层输出正面或负面情感概率。', '按输入、状态更新、分类输出组织。');
compre(85, m3, '说明 LSTM 三个门如何协同更新细胞状态。', '遗忘门先决定旧细胞状态中哪些信息保留；输入门决定当前候选信息写入多少；二者共同得到新的细胞状态。输出门再从新的细胞状态中选择与当前输出相关的信息形成隐藏状态。这样 LSTM 能在保留长期信息的同时吸收新输入。', '三门顺序和角色要清楚。');
compre(86, m3, '如果 RNN 生成文本时反复输出相同词，可能有哪些原因？', '可能原因包括训练数据不足或分布单一、模型容量或训练不足、采样策略过于贪心、温度设置过低、梯度问题导致模型没有学到有效上下文、词表或预处理错误。可通过检查数据、调整采样、改进模型和训练策略排查。', '这是应用分析题。');
compre(87, m3, '为什么 Transformer 后来在很多 NLP 任务中替代 RNN？', 'RNN 需要按时间步串行计算，长距离依赖建模困难。Transformer 使用自注意力直接建模任意位置之间的关系，计算更容易并行，长距离依赖路径更短，因此在大规模语料和硬件加速条件下表现更强。', '为下一章 Attention/Transformer 做衔接。');
compre(88, m3, '如何从考试角度总结 RNN 章节的主线？', '可按“序列数据需求、RNN 基本结构、隐藏状态传递、训练困难、LSTM 改进、GRU 简化、与 CNN/Transformer 对比”来背。先说明为什么需要序列模型，再解释循环结构如何记忆历史，最后回答普通 RNN 的问题和门控结构的解决思路。', '期末速成答题框架。');
draw(89, m3, '画出 RNN 沿时间展开的结构图。', '图中应包含 x1,x2,x3 等序列输入，h1,h2,h3 等隐藏状态，隐藏状态从前一时刻传到后一时刻，并可在每个时间步产生输出 y。标注各时间步共享同一组参数。', '重点是时间展开和参数共享。');
draw(90, m3, '画出 LSTM 单元的三门结构示意图。', '图中应包含细胞状态 Ct 的水平通道，以及遗忘门、输入门、候选状态和输出门。遗忘门控制旧状态保留，输入门控制新信息写入，输出门控制隐藏状态输出。', '不要求公式完全一致，但门控关系要正确。');

const m4 = '注意力与Transformer';
single(91, m4, '注意力机制最核心解决的问题可以概括为？', ['A. 在大量信息中选择与当前任务更相关的信息', 'B. 删除模型所有参数', 'C. 只处理没有顺序的数据', 'D. 让所有输入权重完全相同'], 0, '注意力本质是对不同信息分配不同重要性权重。');
single(92, m4, '自注意力机制中，Q、K、V 通常分别表示？', ['A. Query、Key、Value', 'B. Queue、Kernel、Vector', 'C. Quick、Keep、Vote', 'D. Quality、Kind、Version'], 0, 'Q/K/V 是自注意力计算中的查询、键和值。');
single(93, m4, 'Transformer 相比传统 RNN 的重要优势是？', ['A. 更容易并行计算并建模长距离依赖', 'B. 必须逐时间步串行计算', 'C. 只能处理一个词', 'D. 不需要位置相关信息'], 0, 'Transformer 的自注意力可以并行计算，但仍需要位置编码表达顺序。');
fill(94, m4, '注意力机制输出通常可看作对 Value 的____加权求和。', '权重/注意力', '权重由 Query 和 Key 的相关性决定。');
fill(95, m4, '自注意力中，Query 与 Key 计算相似度后通常经过____函数归一化为权重。', 'Softmax', 'Softmax 将相关性分数变成概率式权重。');
fill(96, m4, 'Encoder-Decoder 框架中，Encoder 负责编码输入，Decoder 负责____输出。', '生成/解码', 'Decoder 根据上下文和已生成内容逐步输出。');
fill(97, m4, 'Transformer 为了表达词序信息，需要加入____编码。', '位置', '自注意力本身不含顺序，需要位置编码补充。');
short(98, m4, '用一句话解释注意力机制。', '注意力机制是在处理当前任务时，为输入中的不同部分分配不同重要性权重，使模型聚焦于更相关的信息、降低无关信息影响的一种方法。', '关键词：权重、相关信息、聚焦。');
short(99, m4, '简述软注意力的计算步骤。' + img('soft-attention'), '软注意力通常包括三步：先计算当前查询与各输入信息之间的相关性得分；再用 Softmax 将得分归一化为注意力权重；最后用这些权重对对应 Value 加权求和，得到上下文向量。', '可写 score、weight、weighted sum。');
short(100, m4, 'Q、K、V 在注意力机制中分别承担什么角色？', 'Query 表示当前要寻找什么信息，Key 表示每个候选信息可被匹配的特征，Value 表示真正要被汇总的内容。Query 与 Key 计算相似度得到权重，再用权重对 Value 加权求和。', '三者角色必须区分。');
short(101, m4, '什么是自注意力机制？' + img('self-attention-qkv'), '自注意力是序列内部各位置之间相互计算注意力的机制。每个词都可以根据自身 Query 与其他词的 Key 计算相关性，再汇总其他位置的 Value，从而获得包含上下文关系的新表示。', '自注意力的输入和被关注对象来自同一序列。');
short(102, m4, '为什么自注意力适合建模长距离依赖？', '自注意力可以让序列中任意两个位置直接计算相关性，无需像 RNN 那样经过很多时间步逐步传递信息。这样长距离词之间的信息路径更短，模型更容易捕捉远距离依赖。', '直接连接是关键。');
short(103, m4, '解释 Encoder-Decoder 框架的基本思想。', 'Encoder 将输入序列编码为上下文表示，Decoder 根据该表示和已生成内容逐步生成输出序列。该框架适合输入和输出长度不固定的任务，如机器翻译、摘要生成和图像描述。', '输入编码，输出解码。');
short(104, m4, 'Seq2Seq 模型主要解决什么问题？', 'Seq2Seq 用编码器-解码器结构处理输入序列到输出序列的映射，适合机器翻译、对话生成、文本摘要等任务。它允许输入和输出长度不同，是序列生成任务的重要框架。', 'Seq2Seq 是序列到序列。');
short(105, m4, '传统注意力和自注意力有什么区别？', '传统注意力常用于 Decoder 查询 Encoder 的输出，关注源序列中与当前生成步骤相关的信息。自注意力则在同一序列内部计算各位置之间的关系，使每个位置都能融合本序列的上下文。', '区别在关注对象来自哪里。');
short(106, m4, 'Transformer 的整体结构可以怎样概括？' + img('transformer-encoder-decoder'), 'Transformer 由 Encoder 和 Decoder 堆叠组成，核心模块包括多头自注意力、前馈网络、残差连接、层归一化和位置编码。Encoder 生成输入表示，Decoder 结合已生成序列和 Encoder 表示生成输出。', '答题时不要只写 Attention，要写完整组件。');
short(107, m4, '为什么 Transformer 需要位置编码？', '自注意力计算本身对输入位置没有天然顺序感，如果打乱词序，纯注意力无法知道原始顺序。位置编码向词向量中加入位置信息，使模型能够区分词语在序列中的先后关系。', '位置编码补足顺序信息。');
short(108, m4, '多头注意力的直观作用是什么？', '多头注意力让模型从多个表示子空间、多个关系角度同时计算注意力。不同头可以关注语法关系、语义关联、距离较近或较远的信息，最后把多头结果合并，提高表达能力。', '不要只说“多个头”，要说多角度关注。');
short(109, m4, 'Transformer 中残差连接和层归一化有什么意义？', '残差连接为信息和梯度提供更直接的通路，缓解深层网络训练困难；层归一化稳定各层输入分布，使训练更平稳。二者共同帮助 Transformer 堆叠更深的网络。', '这是结构组件作用题。');
short(110, m4, '通道注意力和空间注意力分别关注什么？', '通道注意力关注不同特征图或通道的重要性，判断哪些特征类型更有用；空间注意力关注特征图中不同位置的重要性，判断图像哪些区域更关键。二者可结合形成混合注意力模块。', '联系 CNN 注意力。');
short(111, m4, 'CBAM 这类混合注意力模块的基本思路是什么？', 'CBAM 通常依次或组合使用通道注意力和空间注意力，先判断哪些通道重要，再判断哪些空间位置重要，从而对 CNN 特征进行重标定，使网络更加关注关键特征和关键区域。', '无需背结构细节，理解通道+空间即可。');
short(112, m4, '为什么注意力权重具有一定可解释性？', '注意力权重表示模型在当前输出或表示计算中对不同输入部分分配的相对重要性。观察权重较高的位置，可以大致了解模型关注了哪些词、区域或特征。但注意力权重不等同于完整因果解释，只能作为辅助分析。', '最后一句能体现严谨性。');
compre(113, m4, '综合说明从 RNN 到 Attention 再到 Transformer 的演进逻辑。', 'RNN 通过隐藏状态按顺序处理序列，但长距离依赖和并行计算困难。Attention 让模型在生成或表示当前元素时直接关注相关输入，缓解信息瓶颈。Transformer 进一步以自注意力为核心，弱化循环结构，实现更强并行性和长距离关系建模。', '按问题、改进、结果组织。');
compre(114, m4, '以机器翻译为例说明 Encoder-Decoder 加注意力如何工作。', 'Encoder 先把源语言句子编码成一组上下文表示。Decoder 在生成目标语言每个词时，用当前状态作为 Query，与源句各位置 Key 计算注意力权重，再汇总 Value 得到上下文向量。Decoder 结合上下文和已生成词预测下一个词。', '机器翻译是经典应用。');
compre(115, m4, '给出自注意力的一般计算流程。', '首先将输入词向量分别线性变换为 Q、K、V；然后用 Q 与 K 计算相似度分数，常见形式是点积并按维度缩放；接着用 Softmax 得到注意力权重；最后用权重对 V 加权求和，得到每个位置的新表示。', '这是必背流程题。');
compre(116, m4, '为什么 Transformer 适合大规模预训练模型？', 'Transformer 自注意力并行度高，能在硬件上高效处理大规模语料；多层堆叠和多头注意力具有强表达能力；长距离依赖建模更直接；统一的序列建模结构也便于迁移到语言理解、生成、多模态等多种任务。', '从并行、表达、迁移三方面回答。');
compre(117, m4, '如果注意力模型把权重集中在无关位置，可能如何改进？', '可以检查数据标注和预处理是否正确，增加训练数据或数据增强，调整模型容量和正则化，改进位置编码或掩码设置，观察学习率与训练曲线，也可以引入更合适的任务损失或结构先验。', '这是诊断型综合题。');
compre(118, m4, '如何从考试角度总结注意力与 Transformer 章节？', '可按“注意力是什么、如何计算、QKV 含义、自注意力、Encoder-Decoder、Transformer 结构、位置编码、多头注意力、与 RNN 对比”来背。答题时先讲选择重要信息，再讲 Softmax 权重和加权求和，最后落到 Transformer 的并行和长距离依赖优势。', '这是本章速成框架。');
draw(119, m4, '画出自注意力 Q、K、V 的计算示意图。', '图中应从输入词向量出发，通过三组线性变换得到 Q、K、V；用 Q 和 K 计算相似度矩阵，经 Softmax 得到注意力权重；再与 V 加权求和得到输出表示。', '图中要体现 QK 得权重，权重作用于 V。');
draw(120, m4, '画出 Transformer Encoder-Decoder 的简化结构图。', '图中左侧为 Encoder 堆叠，包含多头自注意力和前馈网络；右侧为 Decoder 堆叠，包含 masked self-attention、encoder-decoder attention 和前馈网络；输入端标注词向量与位置编码，输出端接线性层和 Softmax。', '简化图只要模块关系正确即可。');

window.QUIZ_QUESTIONS = questions;
